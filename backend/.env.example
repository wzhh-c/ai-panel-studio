# ============================================================
# 圆桌讨论系统 — 环境变量配置
# 复制此文件为 .env 并填入真实值
# ============================================================

# DeepSeek API（必填）
sk-2c84230d85334a1fba19abaaf16eea7f

# DeepSeek API 地址（可选，默认如下）
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# SQLite 数据库路径（可选，默认为项目根目录下的 panel.db）
DATABASE_URL=sqlite:///./panel.db

# 大模型名称（可选，默认如下）
LLM_MODEL=deepseek-v4-pro
